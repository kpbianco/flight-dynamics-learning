schema_version: 1
batch:
  id: P14
  title: 'Implement P14: Hold Roll and Heading'
  goal: Implement P14 as an interactive MATLAB lesson answering 'What inputs, observable effects, and
    failure modes matter when you hold Roll and Heading?' through visible cause and effect.
sources:
  product: products/flight-dynamics-learning/product.yaml
  requirements:
  - products/flight-dynamics-learning/PRD.md#cap-module-01--complete-learning-slice
  - products/flight-dynamics-learning/acceptance.yaml#CAP-MODULE-01
  - modules/14-hold-roll-and-heading/README.md
  milestone: products/flight-dynamics-learning/milestones/FDL-M4.md
  baseline_commit: 131cc662845614d362133f685e0c159091001f76
  control_plane_repository: kpbianco/portfolio-control
  control_plane_path: products/flight-dynamics-learning/batches/P14.yaml
  control_plane_revision: 4434b282cfb47997d791bd2933faf980453d199a  # pragma: allowlist secret
dependencies:
- P13
scope:
  allowed_paths:
  - modules/14-hold-roll-and-heading/**
  - curriculum/modules.json
  - README.md
  - START_HERE.md
  - modules/README.md
  - bin/learn
  - bin/learn.py
  - docs/plans/P14-*.md
  - docs/evidence/P14-*.md
  - tests/test_p14*.py
  - tests/test_curriculum.py
  - tests/test_learn_cli.py
  forbidden_paths:
  - .github/**
  - .codex/**
  - .agents/**
  - AGENTS.md
  - contracts/**
  - curriculum/phases.json
  - docs/ARCHITECTURE.md
  - docs/IMPLEMENTATION_CONTRACT.md
  - docs/TUTOR_PROTOCOL.md
  - Makefile
acceptance:
- 'Preserve the guiding question: What inputs, observable effects, and failure modes matter when you hold
  Roll and Heading?'
- The learner flow visibly follows read, baseline, one lever, changed view, and mechanism-first explanation.
- experiment.m has a deterministic baseline, labeled plots and metrics, two independent sweeps, and one
  deliberately broken case.
- model.m separates deterministic calculations from presentation and interactive.m exposes meaningful
  controls.
- lesson and walkthrough connect the module to its prerequisites without syntax-first teaching.
- checks include independent numerical invariants, limiting cases, interpretation questions, and teach-back.
- Manifest status becomes implemented only after focused and repository-wide verification passes.
- Evidence distinguishes static validation from MATLAB runtime, UI, numerical, bench, HIL, and field validation.
validation:
  local:
  - MATLAB_LEARNING_VERIFY_PROFILE=contract python3 scripts/verify.py
  - MATLAB_LEARNING_VERIFY_PROFILE=quick python3 -m unittest discover -s tests -v
  - MATLAB_LEARNING_VERIFY_PROFILE=full ./scripts/agent-verify.sh
  focused_tests_to_add:
  - P14 artifact completeness and manifest identity
  - P14 deterministic model and limiting-case checks
  - P14 two-sweep and broken-case regression
  - P14 placeholder and opaque-black-box regression
  required_ci:
  - Portfolio Control validation
  - target Quality workflow
risk:
  class: moderate
  hazards:
  - wrong units or physical interpretation
  - MATLAB mechanics displace the concept
  - opaque toolbox use
  - unrelated module changes
  - unsupported runtime or physical claims
  stop_conditions:
  - required data or toolbox has no approved transparent fallback
  - changes exceed allowed paths
  - expected result conflicts with the governing model
rollback:
  strategy: Revert P14 implementation changes and restore only its manifest status to scaffolded.
evidence:
  output:
  - docs/evidence/P14-<date>.md
  - validation commands and exit results
  - figure, control, metric, and unit inventory
  - acceptance map, runtime inventory, and limitations
  claim_boundary: Module structure, deterministic contracts, and retained outputs only; no MATLAB-runtime,
    UI, numerical-fidelity, bench, HIL, field, or production validation is implied unless separately retained.
